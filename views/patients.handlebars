<nav>
  <div class="blue lighten-1 nav-wrapper">
    <a class="brand-logo center">One Stop Health</a>
    <ul id="nav-mobile" class="left hide-on-med-and-down">
      <li><a href="#">Our Vision</a></li>
      <li><a href="#">About Us</a></li>
      <li><a href="#">Contact</a></li>
    </ul>
  </div>
</nav>
{{patient}}
<div class="container">
  <div class="row">
    <div class="col s5"><span class="flow-text">
        <div id="current-doctors" class="current-patient ">
          <table>
            {{!-- <tr>
              <th>Patient</th>
            </tr> --}}
            <tr>
              <button class="btn waves-effect waves-light" id="patientInfo" type="submit" name="action">Patient Info
                <i class="material-icons right">favorite</i>
              </button></tr>
              {{#each patients}}
              <tr><td>Patient Name: {{this.patient_name}}</td></tr>
              <tr><td>Patient Primary Address {{this.patient_primary_address1}}</td></tr>
              {{/each}}

          </table>
        </div>
      </span>
    </div>
  </div>
</div>
